[project]
name = "primers-catalog"
version = "0.1.0"
description = "Machine-readable catalog of independent technology primers"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"

[tool.uv]
package = false
